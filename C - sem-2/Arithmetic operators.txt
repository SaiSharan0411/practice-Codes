#include <stdio.h>
void main()
{
    int x,y,z;
    printf("enter number a and b");
    scanf("%d%d",&x,&y);
    z=x+y;
    printf("\n addition %d",z);
    z=x-y;
    printf("\n subraction %d",z);
    z=x*y;
    printf("\n multiplication %d",z);
    z=x/y;
    printf("\n division %d",z);
    z=x%y;
    printf("\n modulus %d",z);
    return 0;
}
