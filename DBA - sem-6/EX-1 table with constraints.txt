CREATE TABLE EMPLOYEE (EMPNO INT,NAME VARCHAR(30),JOB VARCHAR(30),DOB DATE,DOJ DATE,SUPERVSR_ID INT,SALARY INT,COMMISSION INT);

ALTER TABLE EMPLOYEE ADD CONSTRAINT CONS_PK PRIMARY KEY(EMPNO);

ALTER TABLE EMPLOYEE ADD CONSTRAINT CONS_CHK CHECK(DOJ>DOB);

ALTER TABLE EMPLOYEE ADD CONSTRAINT CONS_CHK2 CHECK(SALARY>COMMISSION);

ALTER TABLE EMPLOYEE ADD CONSTRAINT CONS_FK FOREIGN KEY(SUPERVSR_ID) REFERENCES EMPLOYEE(EMPNO);

ALTER TABLE EMPLOYEE DROP CONSTRAINT CONS_CHK2;

SELECT CONSTRAINT_NAME,CONSTRAINT_TYPE FROM USER_CONSTRAINTS WHERE TABLE_NAME='EMPLOYEE';