CREATE OR REPLACE FUNCTION QRT(NUM NUMBER) RETURN NUMBER IS
BEGIN
RETURN POWER(NUM,1/3);
END;

 SELECT QRT(27) FROM DUAL;

   QRT(27)
----------
         3

CREATE OR REPLACE FUNCTION ADDS(X NUMBER,Y NUMBER)
RETURN NUMBER IS
Z NUMBER;
BEGIN
Z:=X+Y;
RETURN Z;
END;

SELECT ADDS(20,10) FROM DUAL;

ADDS(20,10)
-----------
         30

CREATE OR REPLACE PACKAGE PK1 IS
FUNCTION QRT(NUM NUMBER) RETURN NUMBER;
FUNCTION ADDS(X NUMBER,Y NUMBER)RETURN NUMBER;

DESC USER_OBJECTS

SELECT OBJECT_NAME,OBJECT_TYPE FROM USER_OBJECTS;



SELECT OBJECT_NAME,OBJECT_TYPE FROM USER_OBJECTS WHERE OBJECT_TYPE='PACKAGE';




CREATE OR REPLACE PACKAGE BODY PK1 IS
FUNCTION QRT(NUM NUMBER) RETURN NUMBER IS
BEGIN
RETURN POWER(NUM,1/3);
END;
FUNCTION ADDS(X NUMBER,Y NUMBER)RETURN NUMBER IS
Z NUMBER;
BEGIN
Z:=X+Y;
RETURN Z;
END;
END PK1;

Package body created.

SQL> SELECT PK1.QRT(64) FROM DUAL;

PK1.QRT(64)
-----------
          4

SQL> SELECT PK1.ADDS(20,30) FROM DUAL;

PK1.ADDS(20,30)
---------------
             50

PACKAGE OVERLOADING:


CREATE OR REPLACE PACKAGE PK1 IS
FUNCTION QRT(NUM NUMBER) RETURN NUMBER;
FUNCTION ADDS(X NUMBER,Y NUMBER)RETURN NUMBER;
FUNCTION ADDS(X NUMBER,Y NUMBER,W NUMBER)RETURN NUMBER;


CREATE OR REPLACE PACKAGE BODY PK1 IS
FUNCTION QRT(NUM NUMBER) RETURN NUMBER IS
BEGIN
RETURN POWER(NUM,1/3);
END;
FUNCTION ADDS(X NUMBER,Y NUMBER)RETURN NUMBER IS
Z NUMBER;
BEGIN
Z:=X+Y;
RETURN Z;
END;
FUNCTION ADDS(X NUMBER,Y NUMBER,W NUMBER)RETURN NUMBER IS
Z NUMBER;
BEGIN
Z:=X+Y+W;
RETURN Z;
END;
END PK1;

SQL> SELECT PK1.ADDS(10,20,30) FROM DUAL;

PK1.ADDS(10,20,30)
------------------
                60

ADDS(X NUMBER,Y NUMBER) RETURN NUMBER IS 


ADDS(X NUMBER,Y NUMBER) RETURN VARCHAR IS 

DECLARE 
N NUMBER;
V VARCHAR;
N:=ADDS(10,20);