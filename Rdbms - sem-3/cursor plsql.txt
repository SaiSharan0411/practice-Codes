DECLARE
REC_ACC ACCOUNT%ROWTYPE;
CURSOR CUR_ACC IS
SELECT * FROM ACCOUNT;
BEGIN
OPEN CUR_ACC;
LOOP
FETCH CUR_ACC INTO REC_CUR
EXIT WHEN CUR_ACC%NOT FOUND||REC_ACC
DBMS_OUTPUT.PUT_LINE('ACCOUNT NUMBER'||REC_ACC);
DBMS_OUTPUT.PUT_LINE('NAME'||REC_ACC.NAME);
END LOOP;
CLOSE CUR_ACC;
END;
/